# YouTube Safety Inspector - Environment Variables
# Copy this file to .env and fill in your values

# YouTube Data API v3 Key (required)
# Get one at: https://console.cloud.google.com/apis/credentials
YOUTUBE_API_KEY=your-youtube-api-key-here

# AI Context Review — eliminates false positives on debunking/educational content
# Set ONE of these for near-perfect accuracy. Without either, heuristic fallback is used.
# OPENAI_API_KEY=your-openai-key-here
# ANTHROPIC_API_KEY=your-anthropic-key-here

# AI Provider: "auto" (default), "openai", "anthropic", or "heuristic" (no API calls)
# AI_PROVIDER=auto

# Optional: Vision analysis requires OPENAI_API_KEY (above) + yt-dlp + ffmpeg installed

# Optional: Chrome Extension ID(s) for CORS lockdown (comma-separated)
# Get your ID from chrome://extensions after loading the extension
# If not set, any extension can access the API (dev mode)
# ALLOWED_EXTENSION_IDS=abcdefghijklmnopabcdefghijklmnop
